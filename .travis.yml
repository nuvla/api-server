language: java

dist: trusty

jdk:
  - openjdk8

cache:
  directories:
  - code/node_modules
  - $HOME/.m2

deploy:
  provider: script
  script: cp .travis.settings.xml $HOME/.m2/settings.xml && mvn deploy
  skip_cleanup: true

env:
  global:
